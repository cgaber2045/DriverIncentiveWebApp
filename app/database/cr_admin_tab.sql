CREATE TABLE admin
(
    first_name  VARCHAR(20),
    mid_name    VARCHAR(20),
    last_name   VARCHAR(20),
    user        VARCHAR(20),
    admin_id    INT,
    phone       INT,
    email       VARCHAR(30),
    pwd         VARCHAR(255),
    date_join   DATE,
    date_leave  DATE
)