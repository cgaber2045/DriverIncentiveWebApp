
CREATE TABLE product
(
    name          VARCHAR(30),
    description   VARCHAR(100),
    price         INT, /* in points */
    product_id    INT,
    sponsor_id    INT,
    image         VARCHAR(100)
)